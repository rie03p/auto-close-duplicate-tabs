# Auto Close Duplicate Tabs

## Features

- Detects and handles duplicate tabs for specified domains (x.com, twitter.com by default)
- Automatically closes newly opened duplicate tabs (e.g. when Cmd+Click or middle-click opens a link)
- Automatically reverts navigation in existing tabs if you navigate to a duplicate page
- Cleans up duplicate tabs across windows, closing redundant ones in other windows
